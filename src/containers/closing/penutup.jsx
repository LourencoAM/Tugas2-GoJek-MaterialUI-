import React, { Component } from "react";
import Closing from "../../components/closing";
import Header from "../../components/header";

export default class Penutup extends Component {
  render() {
    return (
      <div>
        <Header />
        <Closing />
      </div>
    );
  }
}
